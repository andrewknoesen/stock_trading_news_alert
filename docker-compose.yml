services:
  main:
    restart: always
    platform: linux/arm64/v8
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - ALPHAVANTAGE_KEY=<>
      - NEWS_API_KEY=<>
      - TELEGRAM_SUBSCRIBERS=<>
      - TELEGRAM_TOKEN=<>